# ════════════════════════════════════════════════════════════════
# 🔄 CI/CD Pipeline - SQL Library Backend
# ════════════════════════════════════════════════════════════════
# 
# Ce workflow s'exécute automatiquement à chaque push ou pull request
# sur la branche main pour valider le code avant déploiement.
#
# Étapes :
# 1. Vérifie la syntaxe JavaScript
# 2. Installe les dépendances
# 3. Valide que le serveur peut démarrer
#
# ════════════════════════════════════════════════════════════════

name: CI - Backend Validation

# Déclenche le workflow sur push et pull requests vers main
on:
  push:
    branches: [ main ]
    paths:
      - 'backend/**'
      - '.github/workflows/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'backend/**'

# Permissions minimales requises
permissions:
  contents: read

jobs:
  # ════════════════════════════════════════════════════════════════
  # 📦 Job : Validation du Backend
  # ════════════════════════════════════════════════════════════════
  validate-backend:
    name: Valider Backend
    runs-on: ubuntu-latest
    
    # Définir le répertoire de travail pour toutes les étapes
    defaults:
      run:
        working-directory: ./backend

    steps:
      # ────────────────────────────────────────────────────────────
      # 1️⃣ Récupérer le code source
      # ────────────────────────────────────────────────────────────
      - name: 📥 Checkout du code
        uses: actions/checkout@v4

      # ────────────────────────────────────────────────────────────
      # 2️⃣ Installer Node.js (version spécifiée dans package.json)
      # ────────────────────────────────────────────────────────────
      - name: 🟢 Installer Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.19.5'
          cache: 'npm'
          cache-dependency-path: './backend/package-lock.json'

      # ────────────────────────────────────────────────────────────
      # 3️⃣ Installer les dépendances
      # ────────────────────────────────────────────────────────────
      - name: 📦 Installer les dépendances
        run: npm ci

      # ────────────────────────────────────────────────────────────
      # 4️⃣ Valider la syntaxe JavaScript
      # ────────────────────────────────────────────────────────────
      - name: ✅ Vérifier la syntaxe
        run: npm run validate

      # ────────────────────────────────────────────────────────────
      # 5️⃣ Afficher la version Node.js et les infos système
      # ────────────────────────────────────────────────────────────
      - name: 📊 Informations système
        run: |
          echo "Node version: $(node --version)"
          echo "NPM version: $(npm --version)"
          echo "Working directory: $(pwd)"
          echo "Files: $(ls -la)"

  # ════════════════════════════════════════════════════════════════
  # 🧪 Job : Tests (placeholder pour l'avenir)
  # ════════════════════════════════════════════════════════════════
  # 
  # Ce job est commenté car il n'y a pas encore de tests unitaires.
  # Décommentez et adaptez quand vous ajouterez des tests.
  #
  # test-backend:
  #   name: Exécuter les tests
  #   runs-on: ubuntu-latest
  #   needs: validate-backend
  #   
  #   defaults:
  #     run:
  #       working-directory: ./backend
  #
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20.19.5'
  #     - run: npm ci
  #     - run: npm test
  # ════════════════════════════════════════════════════════════════
